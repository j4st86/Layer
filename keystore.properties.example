# Copy to keystore.properties (gitignored) and fill in.
# Keep layer-release.jks and this file together: without them you cannot update the app.
storeFile=layer-release.jks
storePassword=
keyAlias=layer
keyPassword=
